import './bootstrap';
import 'flowbite';

// Initialize Flowbite dropdowns
document.addEventListener('DOMContentLoaded', function() {
    // Flowbite will handle dropdowns automatically via data attributes
});
